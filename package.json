{
  "name": "dotmap",
  "private": true,
  "description": "Headless dotted / matrix map engine for the web",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @dotmap/playground",
    "test": "npm run test -w @dotmap/core && npm run test -w @dotmap/theme",
    "build": "npm run build -w @dotmap/core && npm run build -w @dotmap/react && npm run build -w @dotmap/playground",
    "matrix": "npm run matrix -w @dotmap/core"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
